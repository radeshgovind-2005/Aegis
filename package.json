{
  "name": "aegis",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:dummy-origin": "wrangler dev --config workers/dummy-origin/wrangler.jsonc --port 8788",
    "deploy": "echo 'Refusing: use GitHub Actions workflow_dispatch for production deploys.' && exit 1",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test:boundary": "tsx test/domain/boundary.guard.ts",
    "test:dummy-origin": "cd workers/dummy-origin && npm ci && npm run typecheck && npm run test",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run test:boundary && npm run test:dummy-origin",
    "cf-typegen": "wrangler types",
    "seed": "tsx scripts/seed/index.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.2",
    "eslint-import-resolver-typescript": "^3.10.1",
    "eslint-plugin-import": "^2.32.0",
    "prettier": "^3.8.3",
    "tsx": "^4.21.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.84.0"
  }
}
